name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm test -- --watch=false

      - run: npm run check:vulnerabilities

      - run: npm run check:course-assets

      - run: node scripts/generate-banners.js

      - run: npm run check:build

      - run: npm run check:performance

      - run: node scripts/generate-sitemap.js

      - run: npm run check:internal-links

      - run: node scripts/generate-routes.js

      - run: npm run check:generated-seo

      - run: npm run check:seo

      - run: npm run check:ai-punctuation

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/coderssecret-app/browser

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
